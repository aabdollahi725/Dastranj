<?xml version="1.0" encoding="utf-8"?>
<!--
  PRD §12: android:allowBackup="false". Nothing leaves the device — not to cloud backup,
  not to a device-to-device transfer. PRD §16 records the accepted consequence: uninstalling
  the app or changing phones loses the data outright, because v1 has no backup feature.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
